services:
  dns-tunnel-server:
    build:
      context: tunnels/socks/dnstt
      dockerfile: Dockerfile
    image: dnstt
    container_name: dns-tunnel-server
    command: dnstt-server -udp :53 -privkey-file server.key test.com 127.0.0.1:1080
    restart: unless-stopped
    stop_grace_period: 1s
    networks:
      experiment:
        ipv4_address: ${IPV4_ADDRESS_DNS_TUNNEL_SERVER}
    expose:
      - "53/udp"
      - "53/tcp"
